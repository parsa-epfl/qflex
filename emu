nographic
savevm-external

machine
    virt
    gic-version     max
    virtualization  on

cpu
    max
    pauth-impdef    on

bios
    ${BIOS}

parallel
    none

smp
    ${SMP}

m
    2G

rtc
    clock           vm


drive
    file            ${ROOT}/images/base.qcow2
    format          qcow2
    if              virtio
