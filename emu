nographic

machine
    virt
    gic-version     max
    virtualization  on 

cpu
    max
    pauth-impdef    on

parallel 
    none 

smp
    ${smp}

m
    2G

rtc
    clock           vm



drive
    file            ${root}/images/efi.img
    format          raw
    if              pflash


drive
    file            ${root}/images/varstore.img
    format          raw
    if              pflash

drive
    file            ${root}/images/base.qcow2
    format          qcow2
    if              virtio

savevm-external
