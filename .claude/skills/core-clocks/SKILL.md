---
name: core-clocks
description: Use when debugging **PWQ** (Parallel Warming Quantum) — the intra-machine, across-cores barrier inside ONE simulated client/server machine (one QEMU process). Covers `dynamic_barrier_polling_t`, parallel (MTTCG) vs non-parallel (RR) mode dispatch, virtual time sourcing (`QEMU_CLOCK_VIRTUAL` written by PWQ barrier leader in parallel mode, by upstream icount in RR), `quantum_size` vs `check_period`, per-core IPC (`ip100ns`) from `core_info.csv`, the three wait blocks in `mttcg_cpu_thread_fn`, `stop_request ∈ {0,1,2}` semantics, the snapshot-on-quantum path via `pf_periodic_check_cb`, and where MNQ reads the PWQ-side clock. **Does NOT cover MNQ (Multi-Node Quantum) — the across-machines barrier in `qemu-pdes/` (WWT / `quanta_sync` / `pdes_pause` / `pdes_play`); use the `multi-node` skill for that.** TRIGGER when the user mentions PWQ / dynamic_barrier / quantum_barrier / `tcg-accel-ops-quantum.c` / `tcg-accel-ops-rr.c` / quantum_size / check_period / ip100ns / core_info.csv / `cpu_virtual_time` / `increase_quantum_time` / `quantum_budget` / EXCP_QUANTUM / `pf_periodic_check_cb` / `quantum_checking_callback` / "core is stuck at barrier" / "snapshot not firing from quantum" / parallel vs non-parallel / MTTCG vs RR / is_parallel toggle / "the barrier inside ONE machine". SKIP for **MNQ** / WWT / `qemu-pdes/` / `quanta_sync` / `pdes_pause` / `pdes_play` / `/dev/shm/pdes_*` / "messages from the past" / cross-machine drift (multi-node skill), or how `./qflex` dispatches phases (qflex-commands / executor skills).
---

This skill's content lives in `CORE_CLOCKS.md` next to this directory's root `CLAUDE.md`. Read it now: [../../../CORE_CLOCKS.md](../../../CORE_CLOCKS.md).

PWQ vs MNQ scope table is at the top of the doc — read it first to confirm you're in the right skill. Body covers parallel vs non-parallel split, the PWQ polling-barrier protocol, virtual-time sourcing, per-core IPC math, the three wait blocks, and a debug checklist.
