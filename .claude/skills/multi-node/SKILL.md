---
name: multi-node
description: QFlex multi-node simulation — PDES wire, WWT quantum sync, virtual-time sourcing across phases (icount in FW, Flexus tick in timing), bidirectional pause, distributed snapshots, and what each submodule (parallel-qemu / qemu / qemu/middleware / flexus / WormCacheQFlex) contributes. Use when the user asks about multi-node, PDES, WWT, /dev/shm/pdes_*, neighbor nodes, sampling-unit alignment across nodes, distributed checkpoints, the master node, or pause/resume across QEMU and Flexus.
---

This skill's content lives in `MULTI_NODE.md` next to this directory's root `CLAUDE.md`. Read it now: [../../../MULTI_NODE.md](../../../MULTI_NODE.md).

That doc is the comprehensive cross-cutting overview and links to each submodule's own `MULTI_NODE.md` for the per-submodule view.
